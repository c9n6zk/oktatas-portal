{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "cd ../.. && pnpm install --no-frozen-lockfile",
  "buildCommand": "cd ../.. && pnpm --filter @repo/db exec prisma generate && mkdir -p apps/web/.prisma/client && cp -rL node_modules/.pnpm/@prisma+client@*/node_modules/.prisma/client/. apps/web/.prisma/client/ 2>/dev/null || true; pnpm --filter @repo/web build",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "regions": ["fra1"]
}
